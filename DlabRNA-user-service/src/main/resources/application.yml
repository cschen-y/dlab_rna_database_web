server:
  port: 18081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://115.190.5.32:13306/dlabrna_user?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 1800000   # 30 分钟，必须比 MySQL wait_timeout 小
      idle-timeout: 600000    # 10 分钟
      keepalive-time: 300000  # 5 分钟，定期 ping 连接
      connection-timeout: 30000
  security:
    user:
      name: admin
      password: admin
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.dlab.rna.user.model

# JWT配置
jwt:
 secret: dlabrna-user-service-secret-key
 expiration: 60000 # 24小时